/**
 * @file types.hal
 */

package vendor.gl.hardware.ledcontrol@1.0;

/** LED state. */
enum LedState : uint8_t {
    LED_OFF,
    LED_ON,
    LED_ERR,
};

/** LED number. */
enum Led : uint8_t {
    LED_1        = 0,
    LED_2        = 1,
    LED_3        = 2,
    LED_4        = 3,
    LED_QUANTITY = 4,
};
